schema_version = "1.3"

artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache access log into a JSON summary report."
authors = []
keywords = ["log", "json", "python", "parsing"]

[metadata]

[verifier]
timeout_sec = 120.0
collect = [
  "/logs/verifier/reward.txt",
  "/logs/verifier/ctrf.json"
]

[verifier.env]

[agent]
timeout_sec = 120.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]